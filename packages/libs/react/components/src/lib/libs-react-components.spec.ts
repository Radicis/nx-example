import { libsReactComponents } from './libs-react-components';

describe('libsReactComponents', () => {
  it('should work', () => {
    expect(libsReactComponents()).toEqual('libs-react-components');
  });
});
